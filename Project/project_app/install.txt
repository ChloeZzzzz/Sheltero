#!/bin/sh
sudo npm install express
sudo npm install password
sudo npm install body-parser
sudo npm install express-session --save
sudo npm install password-local
sudo npm install nodemon
sudo npm install dotenv
